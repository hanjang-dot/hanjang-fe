url: ${BO_URL}
env:
  ADMIN_ID: "root"
  ADMIN_PASSWORD: "hanjang-root-dev"
---
- launchApp:
    clearState: true
- runFlow: ../subflows/login.yaml
- tapOn: "로그아웃"
- assertVisible: "한장 BO 로그인"
- openLink: ${BO_URL}/exams
- assertVisible: "한장 BO 로그인"
